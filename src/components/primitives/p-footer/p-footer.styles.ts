import { css } from "@emotion/css";

export const root = css({
    backgroundColor: '#ebebeb',
    display: 'grid',
    justifyContent: 'center',
    textAlign: 'center',
    padding: '40px 30px',

    img: {
        margin: '0 auto'
    }
})