import {css} from '@emotion/css';

export const wrapper = css({
  display: 'grid',
  gap: '10px',

  select: {
    width: '100%',
    height: '30px',
    borderRadius: '8px',
    border: 0,
    minWidth: '190px',
    padding: '0 10px',
  },
});
