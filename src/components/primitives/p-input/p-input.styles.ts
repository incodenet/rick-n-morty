import {css} from '@emotion/css';

export const wrapper = css({
  display: 'grid',
  gap: '10px',

  input: {
    height: '30px',
    borderRadius: '8px',
    border: 0,
    padding: '0 10px',
  },
});
